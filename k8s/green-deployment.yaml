apiVersion: apps/v1
kind: Deployment
metadata:
  name: green-capstone-api
  labels:
    app: capstone-api
    release: green
spec:
  replicas: 1
  selector:
    matchLabels:
      app: capstone-api
      release: green
  template:
    metadata:
      labels:
        app: capstone-api
        release: green
    spec:
      containers:
      - name: capstone-api
        image: mcastellin/udacity-capstone-api:${tagid}
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 3
